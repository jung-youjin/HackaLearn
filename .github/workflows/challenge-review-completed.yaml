name: On Challenge review-completed commented

on:
  issue_comment
  # pull_request:
  # types:
  #  - labeled #ready_for_review]
  #  paths:
  #  - 'teams/**/*.md'
  # pull_request_review_comment:
  #   types: 
  #   - [created, deleted]
  #   paths:
  #   - 'teams/**/*.md'

jobs:
  check-comments:
    name: 'Check comments for /command'
    runs-on: ubuntu-latest
    steps:
      - name: 'Check for command type'
        if: contains(github.event.pull_request.labels.*.name, 'review-completed')
        # if: ${{ github.event.label.name }} == 'reiew-completed'
        id: command
        uses: xt0rted/slash-command-action@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          command: aswasignoff
          reaction: "true"
          reaction-type: "eyes"
          allow-edits: "false"
          permission-level: admin
      - name: 'Act on command'
        run: |
          echo "The command was '${{ steps.command.outputs.command-name }}' on PR #${{ github.event.issue.number }} "

      - name: 'Check for command type'
        if: ${{ github.event.issue.pull_request }} && contains(github.event.pull_request.labels.*.name, 'review-completed')
        id: command
        uses: xt0rted/slash-command-action@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          command: ghasignoff
          reaction: "true"
          reaction-type: "eyes"
          allow-edits: "false"
          permission-level: admin
      - name: 'Act on command ghasignoff'
        run: |
          echo "The command was '${{ steps.command.outputs.command-name }}' on PR #${{ github.event.issue.number }} by @${{ github.event.issue_comment.user.login }}"

      - name: 'Check for command type'
        if: contains(github.event.pull_request.labels.*.name, 'review-completed')
        id: command
        uses: xt0rted/slash-command-action@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          command: socialsignoff
          reaction: "true"
          reaction-type: "eyes"
          allow-edits: "false"
          permission-level: admin
      - name: 'Act on command socialsignoff'
        run: |
          echo "The command was '${{ steps.command.outputs.command-name }}' on PR #${{ github.event.issue.number }}"

      - name: 'Check for command type'
        if: contains(github.event.pull_request.labels.*.name, 'review-completed')
        id: command
        uses: xt0rted/slash-command-action@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          command: reposignoff
          reaction: "true"
          reaction-type: "eyes"
          allow-edits: "false"
          permission-level: admin
      - name: 'Act on command reposignoff'
        run: |
          echo "The command was '${{ steps.command.outputs.command-name }}' on PR #${{ github.event.issue.number }}"

      - name: 'Check for command type'
        if: contains(github.event.pull_request.labels.*.name, 'review-completed')
        id: command
        uses: xt0rted/slash-command-action@v1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          command: rtrosignoff
          reaction: "true"
          reaction-type: "eyes"
          allow-edits: "false"
          permission-level: admin
      - name: 'Act on command retrosignff'
        run: |
          echo "The command was '${{ steps.command.outputs.command-name }}' on PR #${{ github.event.issue.number }}"

          
 # slash-commanding:
 #   name: 'Update label to review-completed when slash command comment used'
 #   runs-on: ubuntu-latest
 #   steps:
 #   - uses: peter-evans/create-or-update-comment@v1
 #     with:
 #     - token: ${{ secrets.PAT }}
 #     - reaction-token: ${{ secrets.PAT }}
 #     - commands: |
 #         aswasignoff
 #         ghasignoff
 #         socialsignoff
 #         reposignoff
 #         retrosignoff
